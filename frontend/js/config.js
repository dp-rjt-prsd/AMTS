/* Front-end configuration.
   Set API_BASE_URL to the API host. An empty string makes every request
   same-origin, which is what you want when the API serves the static files. */

const API_BASE_URL = "http://127.0.0.1:8000";
